# Test / development dependencies.
#   python -m venv .venv && . .venv/bin/activate
#   pip install -r requirements-dev.txt
#
# Verified locally on Python 3.9.6 with: pytest 8.4.2, hypothesis 6.141.1, pydantic 2.13.4
-r requirements.txt

# Agent side (담당자 B)
pytest>=8.0
hypothesis>=6.90

# Core backend + seed (담당자 A) — Lambda 런타임에는 boto3가 기본 포함됨
boto3>=1.34
moto[dynamodb,cognitoidp]>=5.0
joserfc>=1.0   # moto cognito 목킹 의존성
pyyaml>=6.0
faker>=25.0    # scripts/seed 데이터 생성용
